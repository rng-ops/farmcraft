{
  "name": "@farmcraft/shader-watermark",
  "version": "1.0.0",
  "description": "Shader-based watermarking for state encoding and verification",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo 'No tests configured yet' && exit 0"
  },
  "dependencies": {
    "@farmcraft/types": "workspace:*",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
